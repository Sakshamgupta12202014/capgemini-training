server:
  port: 9093

spring:
  application:
    name: DOCTOR-SERVICE

eureka:
  client:
    register-with-eureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://localhost:8761/eureka